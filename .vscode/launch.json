{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch TypeScript",
      "runtimeArgs": ["-r", "ts-node/register"],
      "args": ["${workspaceFolder}/draft.ts"], // Path to your entry file
      "cwd": "${workspaceFolder}"
    }
  ]
}
